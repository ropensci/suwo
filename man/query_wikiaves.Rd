% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query_wikiaves.R
\name{query_wikiaves}
\alias{query_wikiaves}
\title{Query WikiAves for bird media metadata}
\usage{
query_wikiaves(
  species = getOption("suwo_species"),
  format = getOption("suwo_format", c("image", "sound")),
  cores = getOption("suwo_cores", 1),
  pb = getOption("suwo_pb", TRUE),
  verbose = getOption("suwo_verbose", TRUE),
  all_data = getOption("suwo_all_data", FALSE),
  raw_data = getOption("suwo_raw_data", FALSE),
  cookies = Sys.getenv("wikiaves_cookies")
)
}
\arguments{
\item{species}{Character string giving the scientific name of the
species to search for (e.g. \code{"Procnias averano"}). Defaults to
\code{getOption("suwo_species")}.}

\item{format}{Character string, either \code{"image"} or \code{"sound"},
indicating which type of media to query. Defaults to
\code{getOption("suwo_format", c("image", "sound"))}.}

\item{cores}{Numeric. Number of cores to use for parallel processing of
paginated results. Defaults to \code{getOption("suwo_cores", 1)}.}

\item{pb}{Logical. Whether to show a progress bar. Defaults to
\code{getOption("suwo_pb", TRUE)}.}

\item{verbose}{Logical. Whether to print progress and error messages.
Defaults to \code{getOption("suwo_verbose", TRUE)}.}

\item{all_data}{Logical. Whether to return all available columns rather
than the standard \code{suwo} output columns. Defaults to
\code{getOption("suwo_all_data", FALSE)}.}

\item{raw_data}{Logical. Whether to return the raw, unformatted query
output instead of the standardized \code{suwo} output. Defaults to
\code{getOption("suwo_raw_data", FALSE)}.}

\item{cookies}{Single character string of WikiAves authentication
credentials, used to get past Cloudflare's bot protection on every
request this function makes. It is generated by \code{\link[=access_wikiaves]{access_wikiaves()}}
and saved as an environmental variable (\code{Sys.getenv("wikiaves_cookies")}),
which is read by default by \code{query_wikiaves}.}
}
\value{
A data frame of WikiAves observations matching \code{species} and \code{format},
with standardized \code{suwo} columns (or all available columns if
\code{all_data = TRUE}, or the raw query output if \code{raw_data = TRUE}).
Returns \code{invisible(NULL)} if the species is not found, no matching
records exist, or the query otherwise fails.
}
\description{
\code{query_wikiaves} searches for metadata from
\href{https://www.wikiaves.com.br}{WikiAves}, a Brazilian citizen-science
platform and the largest birdwatching community in the country. It
functions as a collaborative, interactive encyclopedia of Brazilian
birds, where users contribute georeferenced photographs and sound
recordings that build a large database used for research and
conservation. \code{query_wikiaves} returns metadata for image or sound
recordings matching a given species, including download links,
locality, author, and verification status.

WikiAves sits behind Cloudflare's bot protection, so this function
requires authentication, which is done with \code{\link[=access_wikiaves]{access_wikiaves()}} (see
Examples).
}
\details{
WikiAves sits behind Cloudflare's bot protection, so \code{cookies} must be
a valid, non-expired credentials string -- see \strong{Do I need to obtain
new credentials before every call?} below.

\strong{Do I need to obtain new credentials before every call?} No -- only
once per Cloudflare session. The \code{cf_clearance} cookie returned by
\code{\link[=access_wikiaves]{access_wikiaves()}} is typically valid for roughly an hour (Cloudflare
does not publish an exact figure, and the duration can vary); after
that, requests made with it will start failing again with an HTTP 403,
and \code{\link[=access_wikiaves]{access_wikiaves()}} simply needs to be called again to obtain a
fresh set of credentials.

A few practical notes on when to refresh credentials:
\itemize{
\item If \code{query_wikiaves} is paging through a species with a large number
of recordings, the session may expire partway through a single call
-- if that happens, call \code{\link[=access_wikiaves]{access_wikiaves()}} again and retry.
\item If some time has passed since the cookies were last obtained, refresh
them before querying rather than waiting for a failure.
\item \code{\link[=access_wikiaves]{access_wikiaves()}} reuses the same browser profile directory by
default, so repeat calls are usually fast and do not require solving
the Cloudflare challenge by hand each time.
}

If valid cookies are not supplied, \code{query_wikiaves} fails gracefully
with an explanatory message and returns \code{invisible(NULL)} rather than
throwing an error.
}
\examples{
if (interactive()) {
# Obtain fresh authentication cookies (only needs to be re-run once the
# cookies expire, roughly every hour):
cookies_live <- access_wikiaves()

# Query sound recordings for a species:
result <- query_wikiaves(
  species = "Procnias averano",
  format = "sound",
  cookies = cookies_live
)

# Query image records instead:
result_images <- query_wikiaves(
  species = "Procnias averano",
  format = "image",
  cookies = cookies_live
)
}

}
\seealso{
\code{\link[=access_wikiaves]{access_wikiaves()}}, which generates the \code{cookies} argument
this function requires.
}
